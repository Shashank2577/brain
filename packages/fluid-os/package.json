{
  "name": "@agent-native/fluid-os",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "Fluid OS — capability registry, manifest types, and inter-app RPC protocol library. Consumed by @agent-native/dispatch (which wires it into its Nitro server) and by templates that need to reference RPC paths or manifest types directly.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./manifest": "./src/manifest/index.ts",
    "./registry": "./src/registry.ts",
    "./rpc/server": "./src/rpc/server.ts",
    "./rpc/client": "./src/rpc/client.ts",
    "./scaffold": "./src/scaffold/template.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nanoid": "^5.1.9",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "catalog:"
  }
}
